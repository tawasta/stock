<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="report_delivery_document" inherit_id="stock.report_delivery_document">

        <!-- State != Done, heading and lines -->
        <xpath
            expr="//table[@name='stock_move_table']/thead//th[@name='th_sm_product']"
            position="before"
        >
            <th name="th_sm_default_location"><strong>Default location</strong></th>
        </xpath>

        <xpath
            expr="//table[@name='stock_move_table']/tbody/tr/td[1]"
            position="before"
        >
            <td name="td_sm_default_location">
                <span t-esc="move.product_id.default_stock_move_location_id.name" />
            </td>
        </xpath>

        <!-- State == Done, heading -->
        <xpath
            expr="//table[@name='stock_move_line_table']/thead//th[@name='th_sml_product']"
            position="before"
        >
            <th name="th_sml_default_location"><strong>Default location</strong></th>
        </xpath>


    </template>

    <!-- State == Done, lines -->
    <template
        id="stock_report_delivery_aggregated_move_lines"
        inherit_id="stock.stock_report_delivery_aggregated_move_lines"
    >
        <xpath expr='//tr[@t-as="line"]/td[1]' position="before">
            <td>
                <t t-if="aggregated_lines[line].get('default_location')">
                    <span t-esc="aggregated_lines[line]['default_location'].name" />
                </t>
            </td>
        </xpath>
    </template>
</odoo>
