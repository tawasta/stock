<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_quant_tree_inherit_short_name" model="ir.ui.view">
        <field name="name">stock.quant.tree.inherit.short.name</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree" />
        <field name="arch" type="xml">
            <field name="location_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_id" position="after">
                <field name="location_id_short_name" optional="hide" />
            </field>
        </field>
    </record>

    <record id="view_stock_quant_tree_editable_inherit_short_name" model="ir.ui.view">
        <field name="name">stock.quant.tree.editable.inherit.short.name</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_editable" />
        <field name="arch" type="xml">
            <field name="location_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_id" position="after">
                <field name="location_id_short_name" optional="hide" />
            </field>
        </field>
    </record>

    <record
        id="view_stock_quant_tree_inventory_editable_inherit_short_name"
        model="ir.ui.view"
    >
        <field
            name="name"
        >stock.quant.inventory.tree.editable.inherit.short.name</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_inventory_editable" />
        <field name="arch" type="xml">
            <field name="location_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_id" position="after">
                <field name="location_id_short_name" optional="hide" />
            </field>
        </field>
    </record>
</odoo>
