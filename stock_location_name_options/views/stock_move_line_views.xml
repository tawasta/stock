<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_move_line_tree_inherit_short_name" model="ir.ui.view">
        <field name="name">stock.move.line.tree.inherit.short.name</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_tree" />
        <field name="arch" type="xml">
            <field name="location_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_id" position="after">
                <field name="location_id_short_name" optional="hide" />
            </field>
            <field name="location_dest_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_dest_id" position="after">
                <field name="location_dest_id_short_name" optional="hide" />
            </field>
        </field>
    </record>

    <record id="view_move_line_tree_detailed_inherit_short_name" model="ir.ui.view">
        <field name="name">stock.move.line.tree.detailed.inherit.short.name</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_tree_detailed" />
        <field name="arch" type="xml">
            <field name="location_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_id" position="after">
                <field
                    name="location_id_short_name"
                    optional="hide"
                    groups="stock.group_stock_multi_locations"
                />
            </field>
            <field name="location_dest_id" position="attributes">
                <attribute name="optional">show</attribute>
            </field>
            <field name="location_dest_id" position="after">
                <field
                    name="location_dest_id_short_name"
                    optional="hide"
                    groups="stock.group_stock_multi_locations"
                />
            </field>
        </field>
    </record>

    <record
        id="view_stock_move_line_detailed_operation_tree_inherit_short_name"
        model="ir.ui.view"
    >
        <field name="name">stock.move.line.operations.tree.inherit.short.name</field>
        <field name="model">stock.move.line</field>
        <field
            name="inherit_id"
            ref="stock.view_stock_move_line_detailed_operation_tree"
        />
        <field name="arch" type="xml">
            <!-- location_id is column_invisible=True upstream by design (restricted
                 field used in quant_id domain). We only add the short-name column. -->
            <field name="location_id" position="after">
                <field
                    name="location_id_short_name"
                    optional="hide"
                    groups="stock.group_stock_multi_locations"
                />
            </field>
            <!-- Make Pick From optional so users can toggle it. -->
            <xpath
                expr="//field[@name='quant_id' and @widget='pick_from']"
                position="attributes"
            >
                <attribute name="optional">show</attribute>
            </xpath>
            <!-- Make the visible location_dest_id optional so users can toggle it. -->
            <xpath
                expr="//field[@name='location_dest_id' and @options]"
                position="attributes"
            >
                <attribute name="optional">show</attribute>
            </xpath>
            <xpath
                expr="//field[@name='location_dest_id' and @options]"
                position="after"
            >
                <field
                    name="location_dest_id_short_name"
                    optional="hide"
                    groups="stock.group_stock_multi_locations"
                />
            </xpath>
        </field>
    </record>
</odoo>