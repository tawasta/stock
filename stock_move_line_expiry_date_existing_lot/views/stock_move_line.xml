<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
	<record id="view_stock_move_line_operation_tree_expiry" model="ir.ui.view">
		<field name="name">stock.move.line.tree.onchange.expiry.date</field>
		<field name="model">stock.move.line</field>
		<field
            name="inherit_id"
            ref="product_expiry.view_stock_move_line_operation_tree_expiry"
        />
		<field name="arch" type="xml">
            <field name="expiration_date" position="attributes">
                <attribute name="column_invisible">1</attribute>
		    </field>

            <field name="expiration_date" position="after">
                <field
                    name="expiration_date_lot"
                    readonly="False"
                    column_invisible="not parent.use_expiration_date or parent.picking_code != 'incoming'"
                    decoration-danger="is_expired"
                />
		    </field>
		</field>
	</record>

    <record id="view_stock_move_line_detailed_operation_tree_expiry" model="ir.ui.view">
		<field name="name">stock.move.line.operations.tree.onchange.expiry.date</field>
		<field name="model">stock.move.line</field>
		<field
            name="inherit_id"
            ref="product_expiry.view_stock_move_line_detailed_operation_tree_expiry"
        />
		<field name="arch" type="xml">
            <field name="expiration_date" position="attributes">
                <attribute name="column_invisible">1</attribute>
		    </field>

            <field name="expiration_date" position="after">
                <field
                    name="expiration_date_lot"
                    readonly="False"
                    column_invisible="context.get('picking_code') != 'incoming'"
                    invisible="tracking == 'none'"
                    decoration-danger="is_expired"
                />
		    </field>
		</field>
	</record>
</odoo>
